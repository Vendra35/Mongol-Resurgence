namespace = mr_dominance
# =================================================================
# MONGOL RESURGENCE - SITUATION LIFECYCLE
# =================================================================
# The spine of the railroad. Every event here is fired deliberately
# by a situation (on_start / on_monthly / on_ended) or by another
# event, via trigger_event_non_silently — never by a date window.
# They therefore carry no dynamic_historical_event block, and no
# trigger of their own beyond option-level gating.
#
# WIRING: the three situation files still need rebuilding against
# the real EU5 field set before these are actually called. Until
# then these events are defined but unreferenced.
#
#   10       situation opens
#   100-105  Phase 1 beats, 107 the Phase 1 setback
#   1 / 2    Phase 1 resolution: unified / still divided
#   120-125  Phase 2 beats, 127 the Phase 2 setback
#   130-132  Phase 3 beats (135 the Four Corners milestone),
#            133 the Phase 3 collapse
#   992-999  AI railroad wars + hidden bookkeeping
# =================================================================
# --- The Eagle of the Steppe: the situation opens ----------------
mr_dominance.10 = {
	type = country_event
	category = situation_event
	title = mr_dominance.10.title
	desc = mr_dominance.10.desc
	outcome = neutral
	fire_only_once = yes
	major = yes
	illustration_tags = {
		10 = armed
		10 = exterior
	}
	option = {
		name = mr_dominance.10.a
		ai_chance = {
			factor = 100
		}
		add_prestige = prestige_mild_bonus
	}
}

# =========================== PHASE 1 =============================
# 1368-1420: the unification of Mongolia.
# --- The Banner Offered ------------------------------------------
# The HUMAN half of the organic birth: an AI horde that takes
# Karakorum converts on the spot, but a player is asked first —
# the same never-force-a-player philosophy as the postponable war
# events. Declining passes the mantle: the 1375 failsafe then
# prefers AI candidates. (A player can still form MGO manually via
# the formable later.)
mr_dominance.11 = {
	type = country_event
	category = situation_event
	title = mr_dominance.11.title
	desc = mr_dominance.11.desc
	outcome = positive
	fire_only_once = yes
	major = yes
	illustration_tags = {
		10 = interior
		10 = happy
	}
	option = {
		name = mr_dominance.11.a
		ai_chance = {
			factor = 100
		}
		form_country = formable_country:MGO_f
		# MGO should be kingdom rank on birth, not a duchy or county.
		c:MGO = {
			if = {
				limit = {
					country_rank_level < 3
				}
				set_country_rank_effect = {
					rank = country_rank:rank_kingdom
				}
			}
			# The AI is set to aggressive for immersion. We use railroding, so personality does not matter, but the AI will still be more aggressive in its diplomacy and war behavior.
			set_personality = ai_personality:ai_aggressive
		}
		# Records that the birth is handled, so neither the organic
		# block nor the 1375 failsafe fires again.
		trigger_event_silently = mr_dominance.998
	}
	option = {
		name = mr_dominance.11.b
		ai_chance = {
			factor = 0
		}
		# The mantle passes. Nothing else — the failsafe will find
		# another khan in 1375.
	}
}

# --- Gathering the Horde ----------------------------------------
mr_dominance.100 = {
	type = country_event
	category = situation_event
	title = mr_dominance.100.title
	desc = mr_dominance.100.desc
	outcome = positive
	fire_only_once = yes
	illustration_tags = {
		10 = armed
		10 = exterior
	}
	option = {
		name = mr_dominance.100.a
		ai_chance = {
			factor = 100
		}
		add_army_tradition = army_tradition_mild_bonus
	}
}

# --- The Steppe Stirs -------------------------------------------
mr_dominance.101 = {
	type = country_event
	category = situation_event
	title = mr_dominance.101.title
	desc = mr_dominance.101.desc
	outcome = neutral
	fire_only_once = yes
	illustration_tags = {
		10 = exterior
		10 = regular
	}
	option = {
		name = mr_dominance.101.a
		ai_chance = {
			factor = 100
		}
		add_prestige = prestige_weak_bonus
	}
}

# --- The Shadow of the Khan -------------------------------------
mr_dominance.102 = {
	type = country_event
	category = situation_event
	title = mr_dominance.102.title
	desc = mr_dominance.102.desc
	outcome = neutral
	fire_only_once = yes
	illustration_tags = {
		10 = interior
		10 = regular
	}
	option = {
		name = mr_dominance.102.a
		ai_chance = {
			factor = 100
		}
		add_government_power = government_power_mild_bonus
	}
}

# --- The Thunder of Hooves --------------------------------------
# The branch point of Phase 1: press the unification by force, or
# consolidate what has already been gathered.
mr_dominance.103 = {
	type = country_event
	category = situation_event
	title = mr_dominance.103.title
	desc = mr_dominance.103.desc
	outcome = neutral
	fire_only_once = yes
	illustration_tags = {
		10 = armed
		10 = exterior
	}
	option = {
		name = mr_dominance.103.a
		ai_chance = {
			factor = 70
		}
		add_army_tradition = army_tradition_severe_bonus
	}
	option = {
		name = mr_dominance.103.b
		ai_chance = {
			factor = 30
		}
		add_government_power = government_power_mild_bonus
		add_prestige = prestige_weak_bonus
	}
}

# --- A New Khan Rises -------------------------------------------
# Fires for MGO the month it first exists. Besides the beat, this
# is where the Great Khan himself is created: a new Borjigin ruler
# with the Timur treatment (rise_of_timur.txt gives Timur the
# timmy_wants_to_play character modifier and the conqueror trait;
# character creation fields follow pirate_events.txt:35 /
# banking.txt:58 / election.txt:165).
mr_dominance.104 = {
	type = country_event
	category = situation_event
	title = mr_dominance.104.title
	desc = mr_dominance.104.desc
	outcome = positive
	fire_only_once = yes
	illustration_tags = {
		10 = interior
		10 = happy
	}
	immediate = {
		hidden_effect = {
			create_character = {
				estate = estate_type:nobles_estate
				first_name = Batu
				age = 30
				culture = root.culture
				religion = root.religion
				dynasty = dynasty:borjigin_dynasty
				adm = {
					80
					100
				}
				dip = {
					60
					100
				}
				mil = {
					90
					100
				}
				save_scope_as = mr_great_khan
				create_in_limbo = yes
			}
			set_new_ruler = scope:mr_great_khan
			scope:mr_great_khan = {
				add_character_modifier = {
					modifier = MR_great_khan
					years = -1
					mode = add_and_extend
				}
				add_character_modifier = {
					modifier = MR_historically_needed
					years = 65
				}
				add_trait = trait:conqueror
				add_trait = trait:tactical_genius
				add_trait = trait:strategist
				add_trait = trait:cruel
			}
		}
	}
	option = {
		name = mr_dominance.104.a
		ai_chance = {
			factor = 100
		}
		add_prestige = prestige_severe_bonus
	}
}

# --- Victory on the Steppe --------------------------------------
mr_dominance.105 = {
	type = country_event
	category = situation_event
	title = mr_dominance.105.title
	desc = mr_dominance.105.desc
	outcome = positive
	fire_only_once = yes
	illustration_tags = {
		10 = armed
		10 = happy
	}
	option = {
		name = mr_dominance.105.a
		ai_chance = {
			factor = 100
		}
		add_prestige = prestige_severe_bonus
		if = {
			limit = {
				has_game_rule = MR_buff_enabled
			}
			add_country_modifier = {
				modifier = MR_unified_mongol_banner
				years = -1
				mode = replace
			}
		}
		else_if = {
			limit = {
				has_game_rule = MR_buff_historical
			}
			add_country_modifier = {
				modifier = MR_mongol_historical_modifier_2
				years = -1
				mode = replace
			}
		}
	}
}

# --- Scattered to the Winds: the Phase 1 setback ----------------
mr_dominance.107 = {
	type = country_event
	category = situation_event
	title = mr_dominance.107.title
	desc = mr_dominance.107.desc
	outcome = negative
	fire_only_once = yes
	illustration_tags = {
		10 = armed
		10 = angry
	}
	option = {
		name = mr_dominance.107.a
		ai_chance = {
			factor = 100
		}
		if = {
			limit = {
				is_ai = yes
			}
			add_country_modifier = {
				modifier = MR_shattered_horde_ai
				years = 25
				mode = add_and_extend
			}
		}
		else = {
			add_country_modifier = {
				modifier = MR_shattered_horde_player
				years = 25
				mode = add_and_extend
			}
		}
		add_prestige = prestige_mild_penalty
	}
}

# --- Phase 1 resolution: the steppe is unified -------------------
mr_dominance.1 = {
	type = country_event
	category = situation_event
	title = mr_dominance.1.title
	desc = mr_dominance.1.desc
	outcome = positive
	fire_only_once = yes
	major = yes
	illustration_tags = {
		10 = armed
		10 = happy
	}
	immediate = {
		hidden_effect = {
			set_global_variable = {
				name = mr_phase_one_complete
				value = yes
			}
		}
	}
	option = {
		name = mr_dominance.1.a
		ai_chance = {
			factor = 100
		}
		add_prestige = prestige_extreme_bonus
		# Reward scales with the buff rule, PD-style (brandenburg_rise
		# on_ending): Terminator gets the full banner, Historical gets
		# the balanced variant, Vanilla gets prestige only.
		if = {
			limit = {
				has_game_rule = MR_buff_enabled
			}
			add_country_modifier = {
				modifier = MR_unified_mongol_banner
				years = -1
				mode = replace
			}
		}
		else_if = {
			limit = {
				has_game_rule = MR_buff_historical
			}
			add_country_modifier = {
				modifier = MR_mongol_historical_modifier_2
				years = -1
				mode = replace
			}
		}
	}
}

# --- Phase 1 resolution: the steppe stays divided ----------------
mr_dominance.2 = {
	type = country_event
	category = situation_event
	title = mr_dominance.2.title
	desc = mr_dominance.2.desc
	outcome = negative
	fire_only_once = yes
	major = yes
	illustration_tags = {
		10 = exterior
		10 = angry
	}
	immediate = {
		hidden_effect = {
			set_global_variable = {
				name = mr_railroad_failed
				value = yes
			}
		}
	}
	option = {
		name = mr_dominance.2.a
		ai_chance = {
			factor = 100
		}
		if = {
			limit = {
				is_ai = yes
			}
			add_country_modifier = {
				modifier = MR_shattered_horde_ai
				years = 25
				mode = add_and_extend
			}
		}
		else = {
			add_country_modifier = {
				modifier = MR_shattered_horde_player
				years = 25
				mode = add_and_extend
			}
		}
		add_prestige = prestige_severe_penalty
	}
}

# =========================== PHASE 2 =============================
# 1420-1550: dominance over the Silk Road, hegemony against the Ming.
# --- Beyond the Steppe ------------------------------------------
# Fires for the claimant at Phase 2 on_start. Like the historical
# succession — Chinggis's heirs kept conquering for two generations
# — a NEW Great Khan of the second generation is enthroned here:
# Adai Khan, the Borjigin khan of the 1420s. Same create_character
# + set_new_ruler shape as Beat 104 (the vanilla Timur treatment;
# multi-trait create_character per hussite_wars.txt:478).
mr_dominance.120 = {
	type = country_event
	category = situation_event
	title = mr_dominance.120.title
	desc = mr_dominance.120.desc
	outcome = neutral
	fire_only_once = yes
	illustration_tags = {
		10 = exterior
		10 = regular
	}
	immediate = {
		hidden_effect = {
			# The crown modifiers pass to the NEW khan — strip them from
			# the outgoing ruler first, or three generations of
			# MR_great_khan would coexist (and the old khan would haunt
			# the court immortal). Shape: grand_embassy.txt:46,
			# ruler ?= { remove_character_modifier = X }.
			ruler ?= {
				remove_character_modifier = MR_great_khan
				remove_character_modifier = MR_historically_needed
			}
			create_character = {
				estate = estate_type:nobles_estate
				first_name = Adai
				age = 25
				culture = root.culture
				religion = root.religion
				dynasty = dynasty:borjigin_dynasty
				adm = {
					70
					100
				}
				dip = {
					60
					100
				}
				mil = {
					90
					100
				}
				save_scope_as = mr_second_khan
				create_in_limbo = yes
			}
			set_new_ruler = scope:mr_second_khan
			scope:mr_second_khan = {
				add_character_modifier = {
					modifier = MR_great_khan
					years = -1
					mode = add_and_extend
				}
				add_character_modifier = {
					modifier = MR_historically_needed
					years = 65
				}
				add_trait = trait:conqueror
				add_trait = trait:born_to_the_saddle
				add_trait = trait:strategist
			}
		}
	}
	option = {
		name = mr_dominance.120.a
		ai_chance = {
			factor = 100
		}
		culture:mongolian_culture = {
			add_cultural_influence = cultural_influence_mild_bonus
		}
	}
}

# --- The Mongol Awakening ---------------------------------------
mr_dominance.121 = {
	type = country_event
	category = situation_event
	title = mr_dominance.121.title
	desc = mr_dominance.121.desc
	outcome = positive
	fire_only_once = yes
	illustration_tags = {
		10 = armed
		10 = exterior
	}
	option = {
		name = mr_dominance.121.a
		ai_chance = {
			factor = 100
		}
		add_prestige = prestige_mild_bonus
	}
}

# --- The Horde at the Gates -------------------------------------
mr_dominance.122 = {
	type = country_event
	category = situation_event
	title = mr_dominance.122.title
	desc = mr_dominance.122.desc
	outcome = neutral
	fire_only_once = yes
	illustration_tags = {
		10 = armed
		10 = exterior
	}
	option = {
		name = mr_dominance.122.a
		ai_chance = {
			factor = 100
		}
		add_army_tradition = army_tradition_mild_bonus
	}
}

# --- The Great Invasion -----------------------------------------
# Phase 2 branch point: commit to the invasion, or consolidate.
mr_dominance.123 = {
	type = country_event
	category = situation_event
	title = mr_dominance.123.title
	desc = mr_dominance.123.desc
	outcome = neutral
	fire_only_once = yes
	major = yes
	illustration_tags = {
		10 = armed
		10 = exterior
	}
	option = {
		name = mr_dominance.123.a
		ai_chance = {
			factor = 70
		}
		add_army_tradition = army_tradition_severe_bonus
	}
	option = {
		name = mr_dominance.123.b
		ai_chance = {
			factor = 30
		}
		add_government_power = government_power_mild_bonus
		add_prestige = prestige_weak_bonus
	}
}

# --- The Empire Rises -------------------------------------------
mr_dominance.124 = {
	type = country_event
	category = situation_event
	title = mr_dominance.124.title
	desc = mr_dominance.124.desc
	outcome = positive
	fire_only_once = yes
	illustration_tags = {
		10 = interior
		10 = happy
	}
	option = {
		name = mr_dominance.124.a
		ai_chance = {
			factor = 100
		}
		add_prestige = prestige_severe_bonus
	}
}

# --- The Yeke Mongol Ulus Proclaimed ----------------------------
# Redundant terminal set, mirroring mr_dominance.1's for Phase 1:
# the authoritative set is in the Phase 2 on_ending success branch
# (it must happen there — see MR_mongol_imperial.txt on_ended note).
mr_dominance.125 = {
	type = country_event
	category = situation_event
	title = mr_dominance.125.title
	desc = mr_dominance.125.desc
	outcome = positive
	fire_only_once = yes
	illustration_tags = {
		10 = armed
		10 = happy
	}
	immediate = {
		hidden_effect = {
			set_global_variable = {
				name = mr_phase_two_complete
				value = yes
			}
		}
	}
	option = {
		name = mr_dominance.125.a
		ai_chance = {
			factor = 100
		}
		add_prestige = prestige_severe_bonus
		if = {
			limit = {
				NOT = {
					has_game_rule = MR_buff_disabled
				}
			}
			add_country_modifier = {
				modifier = MR_empire_fulfilled
				years = -1
				mode = replace
			}
		}
	}
}

# --- The Tide Recedes: the Phase 2 setback ----------------------
mr_dominance.127 = {
	type = country_event
	category = situation_event
	title = mr_dominance.127.title
	desc = mr_dominance.127.desc
	outcome = negative
	fire_only_once = yes
	illustration_tags = {
		10 = armed
		10 = angry
	}
	option = {
		name = mr_dominance.127.a
		ai_chance = {
			factor = 100
		}
		if = {
			limit = {
				is_ai = yes
			}
			add_country_modifier = {
				modifier = MR_imperial_failure_ai
				years = 25
				mode = add_and_extend
			}
		}
		else = {
			add_country_modifier = {
				modifier = MR_imperial_failure_player
				years = 25
				mode = add_and_extend
			}
		}
		add_prestige = prestige_mild_penalty
	}
}

# =========================== PHASE 3 =============================
# 1550-1650: the westward advance of the proclaimed empire.
# --- The Mongol Age ---------------------------------------------
# Fires for the claimant at Phase 3 on_start. The third-generation
# Great Khan takes the throne of the young empire: Altan, the era's
# iconic Mongol ruler. Same Beat 104 shape.
mr_dominance.130 = {
	type = country_event
	category = situation_event
	title = mr_dominance.130.title
	desc = mr_dominance.130.desc
	outcome = positive
	fire_only_once = yes
	illustration_tags = {
		10 = interior
		10 = happy
	}
	immediate = {
		hidden_effect = {
			# Strip the crown modifiers from the outgoing ruler — see
			# mr_dominance.120.
			ruler ?= {
				remove_character_modifier = MR_great_khan
				remove_character_modifier = MR_historically_needed
			}
			create_character = {
				estate = estate_type:nobles_estate
				first_name = Altan
				age = 30
				culture = root.culture
				religion = root.religion
				dynasty = dynasty:borjigin_dynasty
				adm = {
					80
					100
				}
				dip = {
					70
					100
				}
				mil = {
					85
					100
				}
				save_scope_as = mr_third_khan
				create_in_limbo = yes
			}
			set_new_ruler = scope:mr_third_khan
			scope:mr_third_khan = {
				add_character_modifier = {
					modifier = MR_great_khan
					years = -1
					mode = add_and_extend
				}
				add_character_modifier = {
					modifier = MR_historically_needed
					years = 65
				}
				add_trait = trait:tactical_genius
				add_trait = trait:inspiring_leader
				add_trait = trait:expansionist
			}
		}
	}
	option = {
		name = mr_dominance.130.a
		ai_chance = {
			factor = 100
		}
		add_prestige = prestige_severe_bonus
	}
}

# --- The New World Order ----------------------------------------
mr_dominance.131 = {
	type = country_event
	category = situation_event
	title = mr_dominance.131.title
	desc = mr_dominance.131.desc
	outcome = positive
	fire_only_once = yes
	illustration_tags = {
		10 = interior
		10 = regular
	}
	option = {
		name = mr_dominance.131.a
		ai_chance = {
			factor = 100
		}
		add_government_power = government_power_severe_bonus
	}
}

# --- The Four Corners: the khanate capitals reunited -------------
# Mid-Phase-3 milestone, fired by the situation the month one
# claimant owns all four capitals of the old ulus at once —
# Karakorum, Dadu, Samarkand, Sarai. The empire's skeleton stands;
# what remains is flesh.
mr_dominance.135 = {
	type = country_event
	category = situation_event
	title = mr_dominance.135.title
	desc = mr_dominance.135.desc
	outcome = positive
	fire_only_once = yes
	major = yes
	illustration_tags = {
		10 = interior
		10 = happy
	}
	option = {
		name = mr_dominance.135.a
		ai_chance = {
			factor = 100
		}
		add_prestige = prestige_severe_bonus
		add_horde_unity = horde_unity_extreme_bonus
	}
}

# --- The Eternal Empire: the railroad completed -----------------
mr_dominance.132 = {
	type = country_event
	category = situation_event
	title = mr_dominance.132.title
	desc = mr_dominance.132.desc
	outcome = positive
	fire_only_once = yes
	major = yes
	illustration_tags = {
		10 = interior
		10 = happy
	}
	immediate = {
		hidden_effect = {
			set_global_variable = {
				name = mr_railroad_complete
				value = yes
			}
		}
	}
	option = {
		name = mr_dominance.132.a
		ai_chance = {
			factor = 100
		}
		add_prestige = prestige_ultimate_bonus
		if = {
			limit = {
				NOT = {
					has_game_rule = MR_buff_disabled
				}
			}
			add_country_modifier = {
				modifier = MR_mongol_world_order
				years = -1
				mode = replace
			}
		}
	}
}

# --- The Empire Crumbles: the Phase 3 collapse ------------------
mr_dominance.133 = {
	type = country_event
	category = situation_event
	title = mr_dominance.133.title
	desc = mr_dominance.133.desc
	outcome = negative
	fire_only_once = yes
	major = yes
	illustration_tags = {
		10 = interior
		10 = angry
	}
	immediate = {
		hidden_effect = {
			set_global_variable = {
				name = mr_railroad_failed
				value = yes
			}
		}
	}
	option = {
		name = mr_dominance.133.a
		ai_chance = {
			factor = 100
		}
		if = {
			limit = {
				is_ai = yes
			}
			add_country_modifier = {
				modifier = MR_dominance_failure_ai
				years = 25
				mode = add_and_extend
			}
		}
		else = {
			add_country_modifier = {
				modifier = MR_dominance_failure_player
				years = 25
				mode = add_and_extend
			}
		}
		add_prestige = prestige_extreme_penalty
	}
}

# ====================== HIDDEN BOOKKEEPING =======================
# Only .a localisation exists for these two, which is consistent
# with them never being shown: they carry no title or desc.
# --- Birth failsafe bookkeeping ---------------------------------
mr_dominance.998 = {
	type = country_event
	category = situation_event
	hidden = yes
	immediate = {
		set_global_variable = {
			name = mr_birth_failsafe_fired
			value = yes
		}
	}
	option = {
		name = mr_dominance.998.a
	}
}

# --- AI support: military infrastructure -------------------------
# Fired yearly by mr_claimant_manpower_pulse. The AI claimant is a
# steppe horde and tends not to invest in buildings on its own, so
# it gets a nudge. Mirrors PD's pd_brandenburg.999.
mr_dominance.995 = {
	type = country_event
	hidden = yes
	trigger = {
		is_ai = yes
		# tag = is a string comparison and cannot fail when the other
		# country does not exist; this = c:MGE threw "Invalid right side"
		# script errors whenever MGE was not yet on the map.
		OR = {
			tag = MGO
			tag = MGE
		}
	}
	immediate = {
		if = {
			limit = {
				has_advance = kurultai_advance
			}
			random_owned_location = {
				limit = {
					can_build_building = building_type:kurultai
				}
				construct_building = {
					building_type = building_type:kurultai
					cost_multiplier = 0
					cost_multiplier_reason = "MR_dhe_free_buildings"
					instant = yes
				}
			}
		}
		else_if = {
			limit = {
				has_advance = barracks_advance
			}
			random_owned_location = {
				limit = {
					can_build_building = building_type:barracks
				}
				construct_building = {
					building_type = building_type:barracks
					cost_multiplier = 0
					cost_multiplier_reason = "MR_dhe_free_buildings"
					instant = yes
				}
			}
		}
		else_if = {
			limit = {
				has_advance = armory_advance
			}
			random_owned_location = {
				limit = {
					can_build_building = building_type:armory
				}
				construct_building = {
					building_type = building_type:armory
					cost_multiplier = 0
					cost_multiplier_reason = "MR_dhe_free_buildings"
					instant = yes
				}
			}
		}
	}
	option = {
		name = mr_dominance.995.a
	}
}

# --- AI support: keep the horde in the saddle --------------------
# Fired yearly by mr_claimant_army_pulse, peacetime only. Raises
# steppe horse archers at the capital so the claimant can actually
# prosecute the railroad's wars. Mirrors PD's pd_brandenburg.998.
mr_dominance.996 = {
	type = country_event
	hidden = yes
	trigger = {
		is_ai = yes
		at_war = no
		# The AI's manpower must be above zero (economic bottleneck lock)
		monthly_manpower >= 0.1
		# We call the custom macro for the percentage of the army we just created
		MR_percent_of_army_balance = yes
		OR = {
			tag = MGO
			tag = MGE
		}
	}
	immediate = {
		## The AI's army is a mix of steppe units, depending on what it has unlocked. The while loop creates a number of sub-units equal to the count,
		## which is set to 10 for Steppe Horse Archers and 5 for Auxiliary units and Cavalry spawn units. The create_sub_unit_with_owner command creates a sub-unit of the specified type and assigns it to the AI's capital, with the AI as the owner.
		## For Steppe Horse Archers units
		if = {
			limit = {
				has_advance = horse_lords
			}
			capital = {
				while = {
					count = 10
					create_sub_unit_with_owner = {
						type = a_steppe_horse_archers
						owner = root
					}
				}
			}
		}
		## For Auxiliary units
		if = {
			limit = {
				has_advance = horse_lords
			}
			capital = {
				while = {
					count = 5
					create_sub_unit_with_owner = {
						type = a_a_urughs
						owner = root
					}
				}
			}
		}
		## For Cavalry spawn units Age of Revolutions(Age 6) to Age of Reformation(Age 4). 
		## We don't include Age 2 and Age 3 because Steppe Horse Archers units are so much better than them even tho they (Steppe Horse Archers) are belong to Age 1 (Age of Traditions) units.
		if = {
			limit = {
				has_advance = unlock_light_dragoons_advance
			}
			capital = {
				while = {
					count = 5
					create_sub_unit_with_owner = {
						type = a_light_dragoons
						owner = root
					}
				}
			}
		}
		else_if = {
			limit = {
				has_advance = unlock_hussars_advance
			}
			capital = {
				while = {
					count = 5
					create_sub_unit_with_owner = {
						type = a_hussars
						owner = root
					}
				}
			}
		}
		else_if = {
			limit = {
				has_advance = unlock_pistoleers_advance
			}
			capital = {
				while = {
					count = 5
					create_sub_unit_with_owner = {
						type = a_pistoleers
						owner = root
					}
				}
			}
		}
	}
	option = {
		name = mr_dominance.996.a
	}
}

# --- AI railroad: declare the unification war ---
# Fired by the Phase 1 situation once the cooldown matures and a
# target has been selected. This is what makes the railroad a
# railroad: without it the AI simply sits on the steppe. Structure
# mirrors PD's pd_brandenburg.103 exactly: the war declaration
# lives in option A (ai_chance 100), so a HUMAN claimant can pick
# option B and postpone instead of being forced into the war from
# an immediate block. The after block resets the targeting
# variables either way, so the on_monthly picker starts fresh.
mr_dominance.997 = {
	type = country_event
	category = situation_event
	title = mr_dominance.997.title
	desc = mr_dominance.997.desc
	immediate = {
		situation:mongol_resurgence = {
			change_variable = {
				name = mr_conquest_cooldown
				multiply = 0
			}
			var:mr_conquest_target_country = {
				save_scope_as = mr_war_target
			}
		}
	}
	option = {
		name = mr_dominance.997.a
		ai_chance = {
			factor = 100
		}
		declare_war_with_cb = {
			target = scope:mr_war_target
			type = casus_belli:cb_MR_steppe_unification
		}
	}
	# Player-only in practice: the AI never postpones (factor 0).
	option = {
		name = mr_dominance.997.b
		ai_chance = {
			factor = 0
		}
	}
	after = {
		situation:mongol_resurgence = {
			set_variable = {
				name = mr_conquest_target
				value = 0
			}
			# MGO always exists here — it is the country this event just
			# fired for — so pointing the variable back at it is safe.
			# Same shape as PD resetting PD_conquest_target_country to
			# c:BRA in pd_brandenburg.103's after block.
			set_variable = {
				name = mr_conquest_target_country
				value = c:MGO
			}
		}
	}
}

# --- AI railroad, Phase 2: declare the Silk Road war ---
# Fired by the mongol_imperial situation once its cooldown matures
# and a target holding corridor land has been picked. Mirrors PD's
# pd_brandenburg.203 (the Ascension-phase war event) exactly.
mr_dominance.993 = {
	type = country_event
	category = situation_event
	title = mr_dominance.993.title
	desc = mr_dominance.993.desc
	immediate = {
		situation:mongol_imperial = {
			change_variable = {
				name = mr_imp_conquest_cooldown
				multiply = 0
			}
			var:mr_imp_conquest_target_country = {
				save_scope_as = mr_war_target
			}
		}
	}
	option = {
		name = mr_dominance.993.a
		ai_chance = {
			factor = 100
		}
		declare_war_with_cb = {
			target = scope:mr_war_target
			type = casus_belli:cb_MR_silk_road
		}
	}
	# Player-only in practice: the AI never postpones (factor 0).
	option = {
		name = mr_dominance.993.b
		ai_chance = {
			factor = 0
		}
	}
	after = {
		situation:mongol_imperial = {
			set_variable = {
				name = mr_imp_conquest_target
				value = 0
			}
			set_variable = {
				name = mr_imp_conquest_target_country
				value = c:MGO
			}
		}
	}
}

# --- AI railroad, Phase 3: declare the westward / seats war ---
# Fired by the mongol_dominance situation. Same PD-203 shape, but
# the phase has two theatres, so option A picks the CB by where the
# target's land actually lies: the Pontic/Russian/Ural theatre gets
# the westward CB, everything else the silk-road CB. The claimant
# can be MGO or MGE — the after block re-seeds the target variable
# with root, the country this event just fired for.
mr_dominance.992 = {
	type = country_event
	category = situation_event
	title = mr_dominance.992.title
	desc = mr_dominance.992.desc
	immediate = {
		situation:mongol_dominance = {
			change_variable = {
				name = mr_dom_conquest_cooldown
				multiply = 0
			}
			var:mr_dom_conquest_target_country = {
				save_scope_as = mr_war_target
			}
		}
	}
	option = {
		name = mr_dominance.992.a
		ai_chance = {
			factor = 100
		}
		# The western theatre now spans the Golden Horde AND Ilkhanate
		# domains: Rus, the Pontic/Ural steppes, Persia, Mesopotamia
		# and Anatolia all take the westward CB; everything else (the
		# China/Transoxiana seats) takes the silk-road CB.
		if = {
			limit = {
				scope:mr_war_target = {
					OR = {
						any_owned_location = {
							region = region:russian_region
						}
						any_owned_location = {
							region = region:steppes_region
						}
						any_owned_location = {
							region = region:ural_region
						}
						any_owned_location = {
							region = region:persia_region
						}
						any_owned_location = {
							region = region:anatolia_region
						}
						has_presence_in = area:iraq_arabi_area
						has_presence_in = area:armenian_highlands_area
					}
				}
			}
			declare_war_with_cb = {
				target = scope:mr_war_target
				type = casus_belli:cb_MR_westward_advance
			}
		}
		else = {
			declare_war_with_cb = {
				target = scope:mr_war_target
				type = casus_belli:cb_MR_silk_road
			}
		}
	}
	# Player-only in practice: the AI never postpones (factor 0).
	option = {
		name = mr_dominance.992.b
		ai_chance = {
			factor = 0
		}
	}
	after = {
		situation:mongol_dominance = {
			set_variable = {
				name = mr_dom_conquest_target
				value = 0
			}
			set_variable = {
				name = mr_dom_conquest_target_country
				value = root
			}
		}
	}
}

# --- Completion failsafe ---
# The territory transfer itself happens in the situation's on_monthly
# (that is where the location scopes are available). This event gives
# the claimant the means to hold what it was just handed, and records
# that the failsafe fired. Hidden, so the AI resolves it silently.
mr_dominance.999 = {
	type = country_event
	category = situation_event
	hidden = yes
	immediate = {
		add_army_tradition = army_tradition_severe_bonus
		add_government_power = government_power_ultimate_bonus
	}
	option = {
		name = mr_dominance.999.a
	}
}
