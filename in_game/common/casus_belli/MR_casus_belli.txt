# =================================================================
# MONGOL RESURGENCE - CASUS BELLI
# =================================================================
# Rewritten from EU4 format. The previous version used po_annex,
# po_demand_provinces, po_demand_tributary, allowed_provinces_self,
# badboy_factor, province_annex_cost, war_goal = superiority and
# war_name — none of which exist in EU5. EU5 splits the peace terms
# out into common/wargoals; the CB only points at one.
#
# Also renamed: the old CBs were mongol_tributary and
# mongol_imperial, and the latter collided with the Phase 2
# situation key. All three now carry the cb_MR_ prefix.
#
# create_visible/create_enabled are always = no because these are
# granted by the situations rather than picked from the diplomacy
# screen — the same approach PD uses.
# =================================================================
# --- Phase 1: the war to unify the steppe ------------------------
cb_MR_steppe_unification = {
	create_visible = {
		always = no
	}
	create_enabled = {
		always = no
	}
	additional_war_enthusiasm_attacker = 1
	war_goal_type = MR_war_goal_steppe_unification
	ai_will_do = {
		add = {
			desc = "BASE"
			value = 100
		}
	}
}

# --- Phase 2: mastery of the Silk Road ---------------------------
cb_MR_silk_road = {
	create_visible = {
		always = no
	}
	create_enabled = {
		always = no
	}
	additional_war_enthusiasm_attacker = 1
	war_goal_type = MR_war_goal_silk_road
	ai_will_do = {
		add = {
			desc = "BASE"
			value = 100
		}
	}
}

# --- Phase 3: the advance into the west --------------------------
cb_MR_westward_advance = {
	create_visible = {
		always = no
	}
	create_enabled = {
		always = no
	}
	additional_war_enthusiasm_attacker = 1
	war_goal_type = MR_war_goal_westward_advance
	ai_will_do = {
		add = {
			desc = "BASE"
			value = 100
		}
	}
}
