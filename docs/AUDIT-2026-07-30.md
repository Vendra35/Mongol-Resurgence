# AUDIT 2026-07-30 — six fresh lenses (digest; fixes landed same day)

Audited at `a3b279b`; fixes in `c7fde49` (scripts) and `bf29e60`
(harness); harness green after both. The lenses came from the 1066
sister project's discoveries of 2026-07-29/30.

## Verdicts

| Lens | Verdict |
|---|---|
| ?=-swallow + unguarded scope use | CLEAN — the class does not exist in MR (31 save sites walked) |
| Anchored-scan / one-line-block | 1 serious (C1) + 6 minor — fixed/recorded |
| Settle-to-monarchy exposure | mapped (below); `monthly_horde_unity` no-ops silently on settled — vanilla evidence, no log cost |
| Trigger law (flavor vs railroad) | COMPLIANT — 19/19 DHEs have triggers, 0 railroad beats do |
| Reachability incl. delayed form | CLEAN — 91 events, 0 orphans, 0 dangling |
| Fresh eyes on a3b279b | D1 cross-age requires (fixed), D2 pacing drift (fixed), C3 coverage gap (fixed) |

## Fixed same day

- **D1** `mr_yam_riders` cross-age requires → roots on
  `crown_power_advance_discovery` (2748/2748 vanilla requires are
  same-age; law now ENFORCED by the harness, proven by breaking).
- **D2** pacing record aligned to CODE in CLAUDE.md + both phase
  comments (user ruling: the fast pace IS the spec).
- **S1** P1 declare gains the dead-target guard (P1 alone ran declare
  before the fallback). **S2** five bare `capital = {` → `?=`.
  **S3** rival-selection limit gains an exists-guard (was safe only
  via a trigger in another file).
- **C1** harness definitions.txt parse is brace-aware (was dropping
  1337/4150 multi-line province blocks = 38% of the map; proven:
  MR_geo_manchuria pulled from wargoals → 275 uncovered locations
  FAIL). Note: nested geos (khorasan ⊂ persia) cannot individually
  break the union-semantics check — expected, not a flaw.
- **C2** scripted-trigger check: 11-prefix whitelist dropped, 33 → 121
  verified call sites (the Great Partition set was entirely skipped).
- **C3** advances/reforms first-class coverage: age validity, same-age
  requires law, loc resolution (min_count 4) + modifier-tag scan reads
  the new files (222 items).

## Parked (recorded, not fixed)

- **S4** `mr_emergence_candidate` saved 4×, read 0× — dead code or a
  missing link; decide when the emergence block is next touched.
- **S5** `mr_dominance.996` two branches share the `horse_lords` gate —
  second likely meant a different advance; harmless as written.
- **C4-C7** minor line-shape assumptions, `mod_refs` missing the
  character/location removal effects, substring geography probe, zero
  `.gui` coverage — all currently non-firing; take with the next
  harness pass.

## Settle-to-monarchy exposure map (user ruling: transition stays FREE)

Cost of the AI settling is dead tooltip lines, NOT log spam. What
actually turns off:
- **Unwinnable post-settle:** the Torghut and Dzungar end triggers
  demand a Mongol steppe_horde on the goal ground; their reward
  branches never run. The Chahar/Torghut/Dzungar start gates shift
  (Torghut's negative clause flips TRUE post-settle).
- **Cosmetic but permanent:** MGE's map name falls from the composed
  "Great Mongol Empire" (rank_empire_horde requires steppe_horde) to
  the NAME key "Great Mongol State".
- **Low:** several phase beats and the Tumu audience stop reaching the
  claimant (flavor only, no transitions ride on them).
- **[U] to probe in the Great Partition session:** does MGE_f's
  `potential` (third branch requires steppe_horde) block the Phase 2
  proclamation for a settled non-Borjigin MGO? Plus: KAZ instantiation
  with `is_historic = yes`, the `rank_empire_horde` label, and
  `mr_yam_riders` researchability at the 1437 age boundary.

## Clean bills recorded so nobody re-audits them

Trigger-law compliance table, reachability table, the 15 `?=` sites,
multi-tag DHE blocks (100 vanilla precedents), all of `a3b279b`'s
field-for-field verifications (advance/reform/loc/KAZ) — see the full
report in the session transcript of 2026-07-30; the load-bearing
numbers are reproduced above.
